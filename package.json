{
  "name": "vitpago",
  "version": "1.0.0",
  "private": true,
  "description": "Landing VIT PAGO - servidor local (backend/) + funcion serverless para Vercel (api/), ambos sobre el mismo frontend/",
  "scripts": {
    "dev": "node backend/server.js"
  }
}
